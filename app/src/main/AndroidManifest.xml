<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:targetApi="31">
        <activity
            android:name=".quiz.QuizActivity"
            android:exported="false" />
        <activity
            android:name=".authentication.ForgotPassword"
            android:exported="false" />
        <activity
            android:name=".profile.EditProfile"
            android:exported="false" />
        <activity
            android:name=".chatbot.ChatBot"
            android:exported="false" />
        <activity
            android:name=".course.TopicList"
            android:exported="false" />
        <activity
            android:name=".course.TopicView"
            android:exported="false" />
        <activity
            android:name=".course.Description"
            android:exported="false" />
        <activity
            android:name=".authentication.Main"
            android:exported="false" />
        <activity
            android:name=".authentication.SignUp"
            android:exported="false" />
        <activity
            android:name=".authentication.Login"
            android:exported="false" />
        <activity
            android:name=".authentication.Authorization"
            android:exported="false" />
        <activity
            android:name=".authentication.SplashScreen"
            android:exported="true"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.client_id"
            android:value="@string/default_web_client_id" />
    </application>

</manifest>